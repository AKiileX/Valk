# Valk default scan configuration

scan:
  timeout: 120.0
  max_concurrent: 5
  max_retries: 3
  max_tokens: 4096
  delay_min: 0.1
  delay_max: 0.5
  stealth_delay_min: 1.0
  stealth_delay_max: 3.0

output:
  dir: reports/
  format: json

jailbreak:
  level: 2  # 1=safe, 2=moderate, 3=aggressive
